import { FC } from "react";
import { Section, NameSection, NameText} from "./Tag.styled";
let Tag: FC = () => {
    return (
        <Section>
        <NameSection>
          <NameText>Change</NameText>
        </NameSection>
      </Section>
    );
}
export default Tag;